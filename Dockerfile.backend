FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

COPY src /app/src

EXPOSE 8081

CMD ["python", "-c", "from src.assistant.edge_backend import run_edge_backend_server; run_edge_backend_server(host='0.0.0.0', port=8081)"]
